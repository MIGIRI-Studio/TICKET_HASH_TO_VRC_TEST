{
  "name": "ticket-hash-to-vrc",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "fetch": "node --env-file-if-exists=.env scripts/fetch-csv.mjs",
    "make-state": "node scripts/make-state.mjs",
    "build": "node scripts/build-json.mjs",
    "update": "npm run fetch && npm run build",
    "test": "node --test"
  },
  "dependencies": {
    "csv-parse": "^5.6.0",
    "playwright": "^1.50.0"
  }
}
